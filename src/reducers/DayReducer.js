const DayReducer = (state = {
	dayNumber: 0,
	activeties: []
} , action) => {
  switch (action.type) {
    case "what":
      break;
    case "tha hell":
      break;    
  }

  return state;
};

export default DayReducer;
