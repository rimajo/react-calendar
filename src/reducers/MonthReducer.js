const MonthReducer = (state = {
  year: "",
  month: "",
  days: []
}, action) => {
  switch (action.type) {
    case "what":
      break;
    case "tha hell":
      break;    
  }

  return state;
};

export default MonthReducer;
